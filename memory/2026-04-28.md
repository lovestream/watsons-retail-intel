# 2026-04-28 工作日志

## Phase 2 并行化完成

### 新增/修改文件

| 文件 | 改动 |
|------|------|
| `config/parallel.yaml` | 新增 extract_events、analyze_business_impact 配置段 |
| `skills/utils/parallel_runner.py` | 新增 `batch_parallel_map`、`save_checkpoint`、`load_checkpoint`、`clear_checkpoint` |
| `skills/extract_events/extract_events.py` | 文章级并行(Lite→Chat重试)+checkpoint |
| `skills/analyze_business_impact/analyze_business_impact.py` | 事件级分批并行(ARCHIVE→rule,P2/Lite,P1/Chat,P0/Chat低并发)+checkpoint |

### extract_events 改造
- 默认并发 4 (可配置到 6)
- Lite 为默认模型，Chat 为失败重试模型
- 不使用 Thinking 做 JSON 抽取
- 每篇文章失败不影响整体，重试一次后仍失败则 rule fallback
- checkpoint: `extract_events_checkpoint.jsonl` (JSONL, per-article)
- 结果顺序稳定 (按 article index)

### analyze_business_impact 改造
- ARCHIVE: 跳过 LLM，纯规则 (串行)
- P2: LongCat-Flash-Lite, 6 并发
- P1: LongCat-Flash-Chat, 4 并发
- P0: LongCat-Flash-Chat, 2 并发 (低并发)
- checkpoint: `analyze_business_checkpoint.jsonl` (JSONL, per-event)
- 事件输出顺序与输入一致 (scored order)
- 失败回退到规则分析 (fallback_to_rule=true)
- 修复 llm_success_count 返回缺失 bug

### 测试结果 (2026-04-26 数据)
- extract_events: 81.8s, 38/38 文章成功, 0 失败
- score_events: 0.1s (纯规则)
- analyze_business_impact: 59.2s, 56 P2 LLM成功, 0 失败, 53 ARCHIVE 跳过
- 端到端数据完整性: ✅ 事件顺序一致, 0 缺失字段